services:
    registry:
        build: .
        ports:
            - "${REGISTRY_PORT:-8080}:8080"
        environment:
            REGISTRY_KEY_DIR: /data/keys
        volumes:
            - registry-data:/data
        restart: unless-stopped

volumes:
    registry-data:
